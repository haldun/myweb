<%= simple_form_for [theme, asset] do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :content %>
    <%= f.input :path %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
