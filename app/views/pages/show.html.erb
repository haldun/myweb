<%= show_for page do |s| %>
  <%= s.association :site %>
  <%= s.attribute :title %>
  <%= s.attribute :slug %>
  <%= s.association :author %>
  <%= s.attribute :content %>
<% end %>

<%= link_to 'Edit', edit_page_path(page) %> |
<%= link_to 'Back', pages_path %>
