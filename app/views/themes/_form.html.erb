<%= simple_form_for(theme) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :published_at %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
